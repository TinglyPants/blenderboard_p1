export default function CreatePostButton({ onClick }) {
    return (
        <button className="bg-highlight rounded-full mt-[2rem] py-[0.75rem] px-[1.25rem] text-lg">
            Create Post
        </button>
    );
}
